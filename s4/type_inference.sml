(*
in static typed language, a type checker can reject a program befores it runs into some kind of error
in a dynamic typed language, a type checker do so **mostly** when a program runs
*)
(*
ML is both statically typed but also implicitly typed.
*)
(*
type inference problem.
Given every binding or expression a type such that the type checking succeeds
and fail if and only if no solution exists.
*)
